name: ecommerce_forecast
version: 1.0.0
config-version: 2

profile: ecommerce_forecast

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  ecommerce_forecast:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
